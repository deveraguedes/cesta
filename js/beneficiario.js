/* Placeholder for legacy beneficiary script
	 The modal-specific behavior lives in js/beneficiario-modal.js now.
	 Keep this file present (non-empty) so existing pages that include
	 `js/beneficiario.js` do not cause 404s. Add initialization hooks below
	 if you need to reintroduce global behavior later. */

;(function($){
	'use strict';

	// No-op ready handler. Replace or expand as needed.
	$(function(){
		// Placeholder: legacy helpers can be reattached here.
	});

})(jQuery);
